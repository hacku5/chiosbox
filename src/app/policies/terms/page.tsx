import { PolicyViewer } from "@/components/policy-viewer";

export const metadata = {
  title: "Kullanım Koşulları | ChiosBox",
};

export default function TermsPage() {
  return <PolicyViewer slug="terms" />;
}
